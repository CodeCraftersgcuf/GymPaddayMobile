import { apiCall } from "../customApiCall";
import { API_ENDPOINTS } from "../../apiConfig";

// Note: Auth endpoints are primarily POST-based (login, register, etc.)
// This file is prepared for any future GET auth endpoints
