/** Base URL for user live-stream API (matches existing app fetch calls). */
export const LIVE_STREAM_API_BASE = 'https://api.gympaddy.com/api/user';
/** Public route (no /user prefix) used by web player to end stream by channel. */
export const LIVE_STREAM_PUBLIC_BASE = 'https://api.gympaddy.com/api';
